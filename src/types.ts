export type Restaurant = {
  slug: string;
  name: string;
  location: string;
  cuisine: string;
  occasion: string;
  rating: number;
  bestDish: string;
  notes: string;
  review: string;
  website?: string;
};